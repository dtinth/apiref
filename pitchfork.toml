# Dev workflow orchestration

[daemons.shell]
run = "bash -c 'cd packages/shell && SHELL_OUT_DIR=../../_site/assets vp build --watch'"

[daemons.serve]
run = "vp dlx http-server _site --host 0.0.0.0 --port 39757 --gzip"
ready_http = "http://localhost:39757"
